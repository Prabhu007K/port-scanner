# port-scanner
Multi-threaded port scanner with about page, live progress, scan profiles, banner grab, risk hints, UDP DNS demo, heatmap, export &amp; session history. Flask + sockets. Deploy on Render/Railway — not Netlify.
